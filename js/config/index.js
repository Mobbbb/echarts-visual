let App = null

const ENV = 'VISUAL'

// html中card容器的class name
const CARD_WRAP_NAME = 'content-box'

const CONFIG = {
    // 画布基准
    BASE_WIDTH: 1280,
    BASE_HEIGHT: 800,
}

// 渲染类型
const RENDER_TYPE = {
    CHART_RENDER: 'chartRender',
    OTHER_RENDER: 'otherRender',
}

const FETCH_API = {
    DEV: '//localhost:8080',
    TEST: '//test.com.cn',
}

const FETCH_CONFIG = {}
